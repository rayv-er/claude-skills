# brett-prose

General prose style guide. Applies to any extended writing task.

The em dash rule is the one that gets violated most often by default model
behavior. It is absolute: no em dashes, no en dashes, no exceptions.

The "lead with the answer" rule is the second most common failure mode.
Models default to preamble. This skill suppresses it.
