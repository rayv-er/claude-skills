# caddy-site

Checklist for wiring a new public hostname into the homelab reverse proxy.

The `access_tier` naming is confusing: "public" means no Authelia, not
"reachable from the internet." The README note and the skill body both
clarify this because it is easy to misread.
