# Open items

Things handed back and not yet closed. One line each, with an owner, the date it opened, what closing it
looks like, and where possible a command that answers "is this still open" without asking anyone.

The rule that makes this worth keeping: **an item leaves this list by being done or by being decided
against, and both are closures.** An item nobody will ever do, left open, is what turns a list like this
into noise. Move it to Closed with `dropped` and the reason.

Reviewed every Monday morning by the `coach-open-items` scheduled task, which runs the verify commands,
closes what it can prove is done, and reports only what is genuinely still open.

## Open

| Opened | Item | Owner | Closed when | Verify |
|---|---|---|---|---|
| 2026-09-25 | The move from Nextiva to Google Voice launches Friday 16 October 2026 (Jillian's plan, Doc `1KN4UF14ngtEEkPu1XB5zeoxKxKdR6Ne9wynqqQd1nw8`; Corinne coordinates the staff side; kickoff Brett + Corinne 29 Sep). Built and published 2026-09-25 evening: the "Main line" auto attendant answers on 970-236-1788 and mirrors the Nextiva tree (welcome and menu greetings; 1 Ticketing and Events ring group 970-205-9184 with Dana; 2 Rentals and Weddings ring group 970-205-9326 with Amanda and Rebecca; 3 Sales and Marketing, Caressa; 4 Development, Julia; 5 Leadership and Accounting, Brett; star repeats; Mon to Fri 9 to 5 Mountain; after hours the Nextiva script then voicemail to info@ and Dana). Ten seats are licensed (Brett, Jillian, Amanda, Natalie, Julia, Dana, Caressa, Rebecca, Corinne, Lex); info@ and Wade are not. Jillian's plan Doc carries a "Where we stand" section at the top (written 2026-09-25 evening) and the directory sheet's tree and greetings tabs match. Brett's hands, in order: call 970-236-1788 and press through every option and once after 5 p.m. (the console's seven "suggestions" are optional announcements, not errors); sign in to the Admin console again and fix Dana's emergency address (Google Voice, Users, her row, Change addresses; it shows "Registration failed"); decide whether info@ gets a seat and whether Corinne and Lex staff the desk (whoever does joins the Ticketing group, mine to add); get the Nextiva contract term and account PIN (account 5440087, contract 3627563); file the port of 970-349-7487 by about 2 October asking for 16 October (Google Voice, Numbers, then the port flow; it needs the PIN and the signed LOA); cancel Nextiva only after the port lands. Wade's seat is optional. The plan page is https://claude.ai/artifact/Wbsc2g5WbBVfrU3F3jVFFa. | Brett, with Corinne from 29 Sep | the main number answers on Google Voice on 16 Oct and Nextiva is cancelled | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from workspace.gmail_messages where from_address ilike '%voicemail@nextiva.com%' and date > now() - interval '14 days'\""` → nonzero while Nextiva still answers; expect 0 after 16 Oct |
| 2026-09-25 | Between 13:26 and 13:34 MT, commands from this session were replayed on cfta-vm under Brett's login by something that was not this session: the COSA push's second half ran again 90 seconds after it (two duplicate Event Temple events), a probe named `et_incl_probe.py` then deleted the first pair and its notes, and two extra Event Temple syncs ran. This session deleted the second pair on Brett's yes, so both pairs were gone, and recreated the two events once (8945228, 8945229). Two agents writing the same booking at the same time is a hazard until it is known what the second one is: another Claude session on this Mac, a terminal replay, or a scheduled job. | Brett to say what else was running against cfta-vm | the second actor is named and stopped, or confirmed to be Brett | `ssh cfta-vm "sudo journalctl --since today -o short-iso \| grep -c 'et_incl_probe'"` → was 1; any new count means it is still there |
| 2026-09-25 | The branding package Brett built in Claude Design (project `fc79945b-456b-495c-8b2f-7bf726f957b9`) cannot be read from this session: DesignSync needs `/design-login` run once from an interactive Claude Code session on this Mac, or the project sent with "Send to Claude Code Web", or its files (`theme.json`, `styles.css`, `readme.md`, `_ds_manifest.json`) handed over. Until then the BEO Docs, floor plans and email are styled from the 2026 brand guide PDF. | Brett to run `/design-login` or hand over the files | the project's `theme.json` has been read and the three surfaces take their tokens from it | manual: `grep -c "fc79945b" ~/code/clients/cfta/data/scripts/events/beo.py` → 0 until it is wired |
| 2026-09-25 | Amanda's first look at the new event system waits on Brett's note. The three COSA files (the generated BEO Doc, now regenerated from the booking after its information was pushed into Event Temple, the BEO sheet with Event Temple's prices on Final Charges, and the floor plan of her own set-up to scale) are shared to amanda@ as a commenter with no notification, and a note for Brett to send her was drafted in the session. Until the note goes she does not know the files exist. | Brett to send the note | Amanda has commented on, or replied about, the COSA files | manual: comments by amanda@ on Doc `14NQZQfEtTEUdRTDz74smZ_FrMWFZhRpm9ANTGPWKptE`, sheet `1qmsu1tKYZodfk5jjQe1CHSSrBu1pEAvqLPiyK4YdiXg`, or plan `1JXENqunZo23tIjvXWadSYN_zfBJJRord-thQ0k-mOUA`, or a reply in Brett's inbox |
| 2026-09-25 | The floor plan bases cover eleven sheets as complete footprints (Steddy, Grace Atrium, Feldberg East Courtyard, the Atrium and Courtyard together, Miller Upper Lobby, Kinder Padon Gallery, King, Hawk, Jones, Schaul, Comiskey terrace), but some of what is on them waits for a tape or a count: (a) the counts of gallery couches, 20 by 20 and 10 by 10 tents, fire pits, wood bi-folds and bar stanchions, none of which is in Lex's inventory sheet, and the sizes of the couch, fire pit, bi-fold and mobile bar; (b) the Hawk classroom and the terrace, drawn from square footage only, Jones and Schaul, scaled from the 2017 plan, and the East Courtyard's shape and edges, scaled from the 2017 site plan; (c) in the Atrium the stair footprint, the bar counters as built and the door positions, in the Miller lobby the corridor's angle, the box office and the bar alcove. The Feldberg West Courtyard has no page in the capacity book and no drawing with dimensions, so it has no base yet. Each base's key names its own gaps in Patina. The `Floor plan probe (delete me)` file in Brett's My Drive root is the 20 by 14 in page the six medium bases were copied from and can go once they are trusted. | Brett or the event managers with a tape, Lex for the counts | the counts are in the inventory sheet and `KIT`, the tapes are in `ROOMS`, and no room carries a to-measure note | `git -C ~/code/clients/cfta/data show stage5/close-tooling:scripts/events/floor_plan.py \| grep -c '"measure": "[A-Z]'` → was 9, expect 0 |
| 2026-09-22 | Credential rotation has not started. Order is in `docs/runbooks/credential-rotation.md`, with the Google service-account key first because it reads every mailbox, then the vendor keys, then the database roles. The `dbt_runner` password is also a literal in the sync repository's `deploy/dbt-run.sh`. | Brett and admin | every credential in the runbook rotated and the store re-encrypted | manual: ask, and check `git log -1 --format=%as -- secrets/` in cfta-data |
| 2026-09-22 | Seventeen `wine.ticket_sales` rows belong to orders that were cancelled, abandoned, or failed, four of them marked paid. They hold no seats any more. Removing them is a decision nobody has made. | Brett | rows removed, or decided to keep them and this line dropped | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from wine.ticket_sales ts join wordpress.passes p on p.pass_key=ts.pass_key join wordpress.orders o on o.id=p.order_id where o.status in ('checkout-draft','failed','cancelled')\""` → was 17 |
| 2026-09-22 | The daily 08:45 Hetzner push has not completed since before 20 August. The exec bit was lost and git could not see it (`core.fileMode=false`), which killed 33 consecutive runs; the cron now invokes it through `/bin/bash`. The run before that already failed to push all 25 schemas, and that cause is still unknown, so dash.cbarts.center is serving stale data. | me to diagnose, Brett to confirm the dashboard matters | a run completes with no schemas listed as not pushed | `grep 'schema(s) did not push' /tmp/push-to-hetzner.log | tail -1` → expect no output; last run listed 25 |

| 2026-09-22 | Nobody has put a utility bill in the Drive folder since July. The sync job is healthy and ingested 9,114 rows on 2026-09-22; it pulled zero files. Deliberately not exempted so it keeps saying so. | Brett | bills resume, or a decision that they will not and the schema is exempted | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select round(age_hours/24) from ops.v_sync_health where schema_name='utility'\""` → was 54 days |
| 2026-09-22 | Two stale database passwords in n8n, not one. Three live workflows fail authenticating as `cfta_sync`: `health-check`, `summarize-donor-interactions`, `prospect-state-analysis`. One fails as `postgres`: `Idea Deep Research`, 307 errors. The working `cfta_sync` value is in `/opt/cfta/cfta-sync.env` on cfta-vm; the live `postgres` value is not in the sops store and belongs to the rotation. health-check being dead is why none of this reports itself. | Brett | those four workflows record a success | `ssh cfta-vm "sudo runuser -u postgres -- psql -d n8n -At -c \"select count(*) from execution_entity e join workflow_entity w on w.id=e.\\\"workflowId\\\" where w.active and e.status='error' and e.\\\"startedAt\\\" > now() - interval '1 day' and w.name in ('health-check','summarize-donor-interactions','prospect-state-analysis','Idea Deep Research')\""` → was 319 |
| 2026-09-22 | The `dbt-run.sh` path fix exists only as uncommitted work in the sync checkout, and that file also carries the `dbt_runner` password as a literal. The deployed copy was corrected on cfta-vm. | whoever owns that branch | the fix is committed and the literal is gone | `cd ~/code/clients/cfta/sync && git status --porcelain deploy/dbt-run.sh` → expect empty |
| 2026-09-23 | cfta-data `scripts/sync-stripe.py` line 38 carries a live restricted Stripe key (`rk_live_`) as a literal fallback, in Gitea and GitHub history since `007dd5d`. It is read-only but exposes all W+FF shop payment and customer data, and `docs/runbooks/credential-rotation.md` does not mention Stripe at all. | Brett to roll the key in the Stripe dashboard, me to remove the literal and add Stripe to the runbook | key rolled, literal gone, runbook lists Stripe | `grep -c 'rk_live_' ~/code/clients/cfta/data/scripts/sync-stripe.py` → was 1, expect 0 |
| 2026-09-24 | `wff_reconciliation.py` has read every Stripe account since the 9/22 multi-account change, so its Stripe-charges-with-no-shop-order list, open disputes, and fee totals include Event Temple and Planning Pod money. The fix, filtering on `account = 'wff'`, is committed as `b048906` and `9c07696` on `master` and pushed to Gitea and GitHub (2026-09-25). It is NOT deployed: the copy into `/opt/cfta/cfta-sync` and the image rebuild were blocked by the auto-mode production-deploy check, so Brett runs them. The deployed file matched `master` exactly before the fix, so copying it in carries no VM-only drift. | Brett to run the deploy | the image carries the fix | `ssh cfta-vm "grep -c \"account = 'wff'\" /opt/cfta/cfta-sync/wff_reconciliation.py"` → was 0, expect 9 |
| 2026-09-24 | The ntfy token is a literal in 7 of the 12 wrapper scripts in `/usr/local/bin` on cfta-vm, and reading three of them printed it into a session transcript. It belongs in the rotation above, and the wrappers should read `NTFY_TOKEN` from the environment file instead. | Brett to rotate, me to change the wrappers | token rotated and no wrapper carries a literal | `ssh cfta-vm "sudo grep -l 'Bearer tk_' /usr/local/bin/*.sh | wc -l"` → was 7, expect 0 |
| 2026-09-24 | The sync code deployed at `/opt/cfta/cfta-sync` on cfta-vm is not a git checkout and is ahead of every committed branch: its `orchestrator.py` carries `restricted_fund_check`, `_ntfy_title`, lodging, event_inventory, and calendar_warehouse, none of which is on `master`. The 2026-09-24 changes that stopped the sync-error email and dropped inbox triage from the Morning Briefing's freshness list were made there too, so they exist only on the VM. `/etc/cron.d/cfta-sync` had also drifted from `deploy/cfta-sync.cron` before that day, and now also has the two inbox triage lines commented out. A rebuild or redeploy from the repository would silently undo all of it. | whoever reconciles the sync repository | `master` matches what is deployed, and deploys come from a commit | `ssh cfta-vm "sudo md5sum /opt/cfta/cfta-sync/orchestrator.py" ; cd ~/code/clients/cfta/sync && git show origin/master:orchestrator.py | md5` → the two differ today |
| 2026-09-24 | The WooCommerce shop's API key and secret travel in the query string, so any failed request writes both into the log. `wordpress-daily.log` on cfta-vm holds the secret three times, from the 502 on 2026-07-27, and the CFTA Sync Errors email sent that day carried the same error text to Brett's inbox. | Brett to regenerate the key in WooCommerce, me to move the credentials into an auth header | new key issued, no credential in any log | `ssh cfta-vm "sudo grep -c consumer_secret= /var/log/cfta-sync/wordpress-daily.log"` → was 3, expect 0 |
| 2026-09-22 | Every secret in the cfta-sync service environment was printed into a session transcript on this Mac, by running `docker compose config` without filtering the output. The values are the same ones already in `/opt/cfta/cfta-sync.env` and the sops store, so this adds one more copy at `~/.claude/projects/`, on disk, unencrypted. It is a reason to move the rotation up rather than a breach on its own. | Brett and admin | the rotation above is done, or the transcripts holding it are removed | `grep -rl CLOVER_TOKEN ~/.claude/projects/ 2>/dev/null | wc -l` → expect 0 |
| 2026-09-22 | Root's crontab on cfta-vm holds 33 scheduled jobs, including the nightly backup, the spiders, and the two inbox digests, and it is not in version control anywhere. The 2026-07-31 pass moved some of its jobs into `/etc/cron.d/cfta-sync` and stopped partway. A rebuilt VM loses all 33 silently. | Brett to say whether they move into the cron file or get their own tracked file | every line is in a file under git | `ssh cfta-vm "sudo crontab -l -u root | grep -c '^[0-9*]'"` → was 33, expect 0 once they have moved |
| 2026-09-22 | The canonical event dimension is built and verified (1,054 events FY22-FY26, migration 090) but only 77 of the 761 without a QuickBooks customer have any money at stake, and 25 of those carry 87% of it. The review sheet is sorted by money so the read is 25 rows, not 761. The review of proposed renames, creates, and the ten dated-vs-undated splits is the next decision, and nothing may be written to QuickBooks until it happens. | Brett | a reviewed sheet exists and the rename/create list is accepted or rejected | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from events.dim_event where qbo_customer_id is null\""` → was 761, of which 77 carry money |
| 2026-09-22 | `scripts/budget/attribution_scan.py` should target `events.dim_event` instead of raw customer names. It has the tuned matcher, thirteen grades, and stored confirmations; the dimension has the canonical targets. Until they are joined, line-level attribution proposals still come from name matching against 381 customers rather than 1,454 events. | me | attribution_scan proposes an event_key alongside its customer | `grep -c dim_event ~/code/clients/cfta/data/scripts/budget/attribution_scan.py` → expect > 0, was 0 |
| 2026-09-22 | `sync_qbo.py` queries Customer with no WHERE clause, so `qbo.customers` can never hold an inactive record. The close standard's rule to search "inactive included" before creating a customer cannot be satisfied from SQL, which matters the moment the create list above is acted on. | me | the mirror holds inactive customers | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from qbo.customers where not active\""` → was 0 |
| 2026-09-22 | Julia created 2,528 constituents in Bloomerang on 2026-09-15, the day the cleanup sheet was built, under her own name rather than the API key. It tripled the duplicate list, from 343 pairs to 967, and "same name and street" alone went from 2 pairs to 306. Whether that load was meant to land before the deduplication, and whether any of it should be rolled back, is Brett's call. | Brett | a decision that the import stays, or a list of what comes back out | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from bloomerang.constituents where created_date::date = '2026-09-15' and created_by = 'Julia Brazell'\""` → 2,528 |
| 2026-09-22 | A third party writes to Bloomerang that nothing in our documentation mentions: TrueGivers [API] modified 3,222 records through 2026-09-20 and addresses flagged bad went from 464 to 650. Address verification is partly a vendor's job, which changes what the address step of the cleanup program should do. | Brett to confirm the arrangement | the vendor and its scope are written down, and the address step scoped around it | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select max(last_modified_date)::date from bloomerang.constituents where last_modified_by like 'TrueGivers%'\""` |
| 2026-09-22 | Stripe fees have been derived rather than actual on every reconciliation this year, and the actuals are now in the warehouse. The rate used in JE 34438 was 3.441%, taken from the payout rail. The actual rate on Event Temple charges runs 2.86% to 3.06% by month and sat at 2.857% in August. That is a different population from the JE's cutoff window, so it is a true-up to compute rather than a number to assume, but the data exists for the first time. | Brett to decide whether to restate, me to compute it | the derived-fee note comes off the Stripe recon | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select round(100.0*sum(fee)/sum(amount),3) from stripe.balance_transactions where account='main' and type='charge' and created_at >= '2026-08-01' and created_at < '2026-09-01'\""` -> 2.857 |
| 2026-09-22 | FY26 corporate sponsorship revenue is 7,000.00 on two lines, both W+FF, both with no customer on them. At least ten signed 2026 business sponsorship agreements exist in Drive, including SHM Architects at 15,000 and The Beckwith, and none is visible in the ledger by name. There is also no sponsorship or underwriting account in the chart at all; the two lines sit in `4040.12 Corporate`. The agreements run January to December and the fiscal year ends in October, so some of this is timing, but not all of it. | Brett and the CBO | every signed agreement is either booked or explained | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from qbo.general_ledger where txn_date >= '2025-11-01' and account_name='4040.12 Corporate' and coalesce(name,'')=''\""` -> was 2 unnamed lines |
| 2026-09-22 | `workspace.drive_files.text_path` points at extracted text that no longer exists on disk. Nothing is at `/data/drive_text` in the container or on the host, so a column that says a document is searchable is lying, and any search built on it silently returns nothing. Found while reading the sponsorship agreements, which had to be pulled through the Drive API instead. | me | the column is either repopulated or dropped | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm cfta-sync sh -c 'ls /data/drive_text 2>&1'"` -> no such directory |
| 2026-09-23 | `deploy-overwrite-guard.py` blocks installing a file that does not exist on the server yet, where there is nothing to overwrite and nothing to lose. It fired twice today on a new script. The fix is to let a create through, or to ask for a backup only when the destination exists. A guard that fires on safe work trains people to reach for GUARD_OK. | me | a new-file install passes without an override | `grep -n 'writes_into_deployment' ~/code/claude-skills/hooks/deploy-overwrite-guard.py` |
| 2026-09-23 | 22,627.80 of FY26 giving is still on 4030.13 Temporarily Restricted. Everything else was released or reclassified on 2026-09-23 by Bloomerang designation (releases 34777 to 34780 and 34783, the gallery on its donors; reclassifications 34781, 34782). **Held by decision:** the Walter Family Foundation's 20,000 (Brett: leave restricted). **Waiting on a fact:** two Bloomerang payouts with no donor, 48.10 on 15 July and 2,419.40 on 27 July, which Bloomerang's payout report would split; a 110.30 tip and a 50.00 Humanitix payout for Apres Jazz that are not gifts. | Brett: the payout report, where the two non-gifts go | each line released, reclassified, or held with a named reason | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select sum(amount) from qbo.general_ledger where txn_date >= '2025-11-01' and account_name ~ '^4030'\""` -> 22,627.80 once the warehouse syncs |
| 2026-09-23 | Two FY26 bar sales-tax filings do not fit the bar sales they cover. May remitted 94.08 against 6,070.50 of bar sales, where 9.4/109.4 would be 521.60. July remitted 6,139.69 more than bar tax alone; June's entry netted out 826.70 of Alpenglow food-vendor tax and July may not have. Found while spreading the carve-out; the differences now sit visibly on Programming - Overhead, one line a month. | Brett or the CBO, against the filed returns | each difference is explained or corrected | the residual lines on `YYYY-MM-bartax` for 2026-05 and 2026-07, description beginning "bar sales tax - filed" |
| 2026-09-24 | BEOs from Prism: `scripts/events/beo.py` in cfta-data builds a Google Doc per show in Brett's BEOs folder, and the Floodgate Operators pilot is built. On 2026-09-24 Brett had the Contract, Lodging, Hospitality (dropdowns) and BEO (URL) custom fields created in Prism, and the same day BEO links went on Firebird, Ditchwalkers, Rocky Horror, and Floodgate, with Lodging Needed on Floodgate; the rest are empty until the team fills them, and the Prism connector in Claude can fill them with Brett's yes per change. What remains of the old admin steps: nothing for the BEO link, which lives in that URL field, like the existing "Facebook URL", so each event can link its Doc; and create a Prism API key with Read & Write (write-events) so the generator can attach the BEO to the event. The key goes into the sops store as PRISM_RW_API_KEY, never into chat. | Brett or a Prism admin | the field exists and the key is in the sops store | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from prism.event_financials f, jsonb_array_elements(f.raw::jsonb -> 'customFields') c where c ->> 'label' = 'BEO'\""` → was 0; `grep -c PRISM_RW_API_KEY ~/.config/cfta/db.env` → was 0 |
| 2026-09-24 | After rentals and co-pros got their revenue on 2026-09-24 (39,771.25 onto 11 events), six dated programming events still carry costs and no revenue: the CBCS Band Concert (3,031), Graduation (2,053), and Jazz (792), which may have been free community use; SOD Nutcracker Rehearsals (1,443), which belong with the Nutcracker; the 2025 Arts Ball's FY26 costs (6,549); and Event Staff Training (949), which is not an event and can stay. Smaller: 6,480 of December revenue on Rentals - Overhead from FY25 deposits, composition partly documented; 614 of March hosted bar unattributed (Winter Tech Forum?); Eleven Experience's 946.72 hosted bar at Alpenphunk on the renter; and whether Blackline's 16,200 deposit, now spread over the nine Alpenglow shows, was netted against its 4,200 per-show bills. | Brett | each placed on its event, confirmed free or co-produced, or left with a reason | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"with r as (select regexp_replace(g.cust_name, '^((Programming:Rentals|Programming):[^:]+).*$', '\\1') ev, a.classification c, g.amount from qbo.general_ledger g join qbo.accounts a on a.account_id = g.account_id where g.txn_date >= '2025-11-01' and g.cust_name ~ '^Programming:') select count(*) from (select ev from r where split_part(ev, ':', -1) ~ '^20[0-9]{2}[.]' group by ev having sum(amount) filter (where c = 'Expense') > 500 and coalesce(sum(amount) filter (where c = 'Revenue'), 0) = 0) x\""` → was 6 |
| 2026-09-23 | The monthly restricted fund check (`restricted_fund_check.py` in cfta-sync, cron the 2nd at 07:20, mail to Brett only) found on its first run where QuickBooks and Bloomerang still disagree, beyond the 22,627.80 above. **A grant:** Colorado Creative Industries 7,500 on 30 June, booked to 4020.11 restricted, Unrestricted in Bloomerang; the award letter decides which is right. **Designated in Bloomerang, booked unrestricted:** six lines, 11,891.71, of which 10,331.18 is one donor's 5,165.59 Alpenglow gift entered on 2 and 18 December, likely once too many. **Restricted grants on 4020.11 not yet released:** Mountain Words 2,740 and Dance 3,460. **In Bloomerang to a restricted fund, not found in QuickBooks:** seven gifts, the largest Inn Old Town 5,500 to Mountain Words. | Brett | the check reads 0 to look at, or every remaining line is held with a named reason | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm cfta-sync python restricted_fund_check.py 2>/dev/null | head -1"` -> was "10 to look at" |
| 2026-09-24 | Floodgate Operators' Prism contact for Rebecca Vehik gives her email as `rebecca@crestedbutte.org`; her Event Temple login is `rebecca@crestedbuttearts.org`. BEOs now take staff emails from Event Temple, so the BEO is right, but Prism's own PDFs and emails still carry the typo. It is the only CFTA staff email in Prism that disagrees. | Brett, or whoever keeps Prism contacts | the Prism contact is corrected and the nightly sync has run | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from prism.event_contacts ec join eventtemple.users u on lower(u.name) = lower(ec.name) where ec.company ~* '^(center for|cfta)' and lower(ec.email) <> lower(u.email)\""` → was 1, expect 0 |
| 2026-09-24 | The event SOP, draft 0.1, is written: Google Doc "Event SOP: inquiry to close" in Brett's My Drive (id `1awklNs29J8qZNnhk9Siym6oUBP2oZvt5DgeZMukXlO0`), shared by Brett with Jillian and Natalie as editors, at version 0.2 since the same day. It lists 14 open decisions, the first being who owns the document. It is the only copy and is edited in place, never regenerated. | Brett to review and name an owner | the change log shows a version after 0.1 | manual: open the Doc and read its change log |
| 2026-09-24 | Natalie's Monday brief now renders in Gmail (inline styles, full document; fd664e5 on cfta-sync branch `land/natalie-brief-inline`, local only, stacked on `land/beo-source-fields`, so it belongs to the drift item above). Deployed and the image rebuilt; backup `/opt/cfta/backups/natalie_monday_brief.py.bak-20260924-inline`. It is not held: under `CFTA_MAIL_ONLY` its `To` comes from `mail_guard.allow()`, which returns the allowlist, so the first styled copy reaches Brett at the next Monday run, which fires at 00:00 MT rather than 06:00. Releasing it to Natalie is not an environment change yet: `allow()` never reads `CFTA_MAIL_RELEASE`, and the brief passes `__name__` (`__main__` under `cfta-run.sh`) as its label, so it needs `event_readiness.py`'s `recipients()` and an explicit label first. | Brett to decide on release, me to make that change once he says yes | the brief is delivered to Natalie, or a decision that it stays with Brett | `ssh cfta-vm "sudo grep -c 'sent to natalie' /var/log/cfta-sync/natalie-brief.log"` → was 0 |
| 2026-09-25 | The old Event Temple sync script, `/opt/cfta/scripts/sync-eventtemple.py` on cfta-vm, no longer runs: root's 07:30 crontab line now runs the cfta-sync `eventtemple` module instead (backup `/opt/cfta/backups/root-crontab.bak-20260925-0714`), because the script deleted and rebuilt bookings and events without custom fields, operations users, setup styles, or serving times every morning. It stays on disk as a second copy until the module has a week of clean daily runs. Same decision for `/opt/cfta/cfta-sync/cfta-sync/`, a 36-file copy of the whole sync from 2026-04-23 nested inside the deployed tree (owner brett, not in the image, not run by anything; found 2026-09-25). | Brett to say delete, me to do it | both are gone and the module has run clean daily for a week | `ssh cfta-vm "sudo ls -d /opt/cfta/scripts/sync-eventtemple.py /opt/cfta/cfta-sync/cfta-sync 2>/dev/null | wc -l"` → was 2, expect 0 |
| 2026-09-25 | The lodging sync now reads every staff mailbox and the Gmail archive, with cancellations (2b1707e on cfta-sync branch `land/lodging-all-mailboxes`, off `master`, local only). Deployed at Brett's yes, backup `/opt/cfta/backups/sync_lodging.py.bak-20260925-allmailboxes`; `lodging.reservations` went from 31 to 58 and six June rooms were corrected to one night. Long stays now attach to the festival they overlap (cfta-data migration 093, Brett's decision 2026-09-25). Left: the branch is not on `master` (the drift item above). | whoever reconciles the sync repository | the commit is on `master` | `cd ~/code/clients/cfta/sync && git branch -a --contains 2b1707e` → lists `master` once landed |
| 2026-09-25 | The March 2026 sandbox is still alive: the "BEO Web App (SANDBOX)" Apps Script on the CFTA Sandbox shared drive rewrites `BEO_Master-SANDBOX` every 30 to 60 minutes (137 revisions, 25 a day) and `CFTA Events Master (SANDBOX)` daily at 15:07 UTC, as Brett, against calendar ids that no longer match anything. Nobody reads either; the v2 sandbox calendars and events.dim_event replaced them. Its time triggers live in the script project, which only its owner can open. Both sheets were moved into `_archive (do not use)` at the sandbox root on 2026-09-25 at Brett's yes, so nobody mistakes them for the new events master; the script keeps writing them there by id. | Brett | the triggers are off, or the script is kept on purpose and this line dropped | check the sheet's revision count stops growing: Drive revisions of `1kSxsRepQNA94DVprD5JELcwuJyIyy8kC6vxrcsLWtp4` → was 137 on 2026-09-25 and rising |
| 2026-09-25 | The daily Event Temple and Humanitix syncs still run at 07:30 and 07:00, after the 03:00 refresh of `analytics.program_master`, so the 06:30 dim_event build and the 06:45 readiness email read bookings a day old. The move to 02:30 and 02:45 is scripted and committed (cfta-sync 01e4c8f on `land/beo-source-fields`, `deploy/move-et-humanitix-0230.sh`: it backs up root's crontab and `/etc/cron.d/cfta-sync` first, aborts if either line is not where it expects, and prints the diffs), but the session's permission mode refused to run it on the VM. One command: `git -C ~/code/clients/cfta/sync show land/beo-source-fields:deploy/move-et-humanitix-0230.sh \| ssh cfta-vm 'cat > /tmp/move_cron.sh && sudo bash /tmp/move_cron.sh'`. Cronicle's "Sync: Humanitix" keeps firing at 07:00 as well; harmless, consolidate later. | Brett | both cron lines show the new times | `ssh cfta-vm "sudo crontab -l \| grep -c '^30 2 .*mod eventtemple'; grep -c '^45 2 .*mod humanitix' /etc/cron.d/cfta-sync"` → 1 and 1 |
| 2026-09-25 | FY26 hotel charges are on their events (63 lines re-tagged at Brett's yes, cfta-data 6c2a61a, `scripts/qbo/lodging_tag_plan.py`). Three lodging lines sit on the wrong customer with no right one to move to: Coltan Scrivner 1,208.60 "Survival Fest Reimbursement" on Apres Jazz (no Survival Fest customer), Britney Kuehm 391.62 CCI conference hotel on "Welcome to Britney's Circus" (staff professional development, not an event), and Almont Resort 8,181 "WFF event labor + tips" on Hand Turkey (not lodging, looks like W+FF). Small unknowns left in place: 64.66 on ROMP, 34.11 on the plein air course, 211.78 in the Chantil block, and Wild Hare's 300.82 x2 with the untagged -293.85 x2 credits (Corinne would know). The opera rooms are all on Opera Lollipops; they also cover CopyCat Returns and Cowboy Songs if Brett wants them split. | Brett to decide each; me to write it | each line moved, or kept with a reason | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from qbo.general_ledger where txn_id in ('31044','34287','34330') and cust_name ~ 'Apres Jazz\|Britney\|Hand Turkey'\""` → was 3 |
| 2026-09-25 | `lodging.v_reservation_events` matches stays only against room-calendar shows marked 🎭, 🍽️ or Alpenglow, so it put the NRO Alumni String Quartet (7/14) on Wine + Food and Hiroya Tsukamoto (Summer Classics 7/21) on Madeline Hawthorne. The QuickBooks tags were set from documents, not the view, so the books are right; the Monday brief and readiness email read the view. The fix is candidates from `events.v_event_spine` (which carries Prism and Summer Classics shows) instead of calendar emoji. | me | the 7/14 quartet party reads National Repertory Orchestra | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select assigned_event from lodging.v_reservation_events where arrival='2026-07-14'\""` → was 🍽️ Culinary: W+FF |
| 2026-09-25 | `/var/lib/postgresql/je-fix` on cfta-vm has been owned by an unknown uid (501, a Mac copy) since 2026-09-23, so the `postgres` user can no longer create files there. Existing files, including the QBO audit log, still append; new files fail, which is why `qbo_attr_assign.py` could not write its result file on 2026-09-25 (it went to /tmp instead). One `chown postgres` fixes it. | Brett to say, me to do it | the folder is owned by postgres | `ssh cfta-vm "sudo stat -c %U /var/lib/postgresql/je-fix"` → was UNKNOWN, expect postgres |
| 2026-09-25 | The Kumpuris 2025 tax receipt (`cfta/cowork/donor-statements/2025/`) states that no goods or services were provided for any of the four gifts. Bloomerang records $0 non-deductible on each, but QuickBooks books the July $6,000 check (invoice 2025-AB-43) as an Arts Ball "paddle raise, live auction, or silent auction" contribution. A paddle raise is fully deductible; an auction purchase is deductible only above fair market value, and the receipt would need the item's value on that line and a restated total. | Brett | Brett confirms the $6,000 was a paddle raise, or gives the auction item's value and the receipt is restated | `grep -c 'Open question for the tax receipt' ~/code/clients/cfta/cowork/donor-statements/2025/kumpuris-2025-giving-summary.md` → 0 once the section is removed or resolved |
| 2026-09-25 | The Event Temple settings audit (every page under Settings, read against the help centre) found options the team is not using. Done at Brett's yes on 2026-09-25 evening: page size to Letter, Show excluded spaces on, and nine catalog fixes (four prices, Partner Class hourly, a $2,000 deposit tier, Popcorn Machine, the duplicate alcohol deposit item archived); space conflict was already Prevent with leads excluded (the audit had misread it as Allow). Still Brett's call: Grace Atrium and Miller Upper Lobby stay one space (127 Atrium events in 12 months, none lobby-only, so the recommendation is keep); no Event Templates and no Packages, so every booking starts from a blank event and bar packages are typed each time (Amanda's own complaint); no Webhooks, so the warehouse waits for the daily pull instead of hearing about a change; Calendar Integration is not linked for anyone; the organization owner and billing contact is Caressa, not Brett; the "Center BEO" document template (type Banquet Event Order) exists unused; Tracy Hastings holds a read-only seat; the subscription ($4,500 a year, Unlimited) renews 20 November 2026. The FY26 rentals budget, the one item Brett approved, is entered (memory note eventtemple-admin-setup). The evidence and a recommendation per setting, with the catalog check (nine menu lines off the 2026 guides) and the rental guide review, are in the doc Event Temple settings review (https://claude.ai/code/artifact/2b3728a0-2b20-4f21-829d-0749e17fdb68), written 2026-09-25 evening at Brett's ask. | Brett | each item changed, or kept with a reason | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm -T cfta-sync python -c \"import os,requests;print(len(requests.get('https://api.eventtemple.com/v2/spaces',headers={'X-API-KEY':os.environ['EVENTTEMPLE_API_KEY']},params={'page[size]':100}).json()['data']))\""` → 13 while the Atrium and the lobby are one space, 14 once split; the rest are UI settings the API does not expose, closed by Brett's word |
| 2026-09-25 | `scripts/events/et_push.py` (cfta-data, stage5/close-tooling) still trusts a failed POST /events: the API created records on a 401 once (the COSA duplicate pair of 2026-09-25), and the pusher's only defence is the dedup on rerun. The fix is a read of the booking's `events_count` before and after each create, stopping with a warning when the count rose despite an error. The forked session that found it could not edit that worktree (the worktree hook), so it is not written yet. | me | the guard is in the file and a dry run prints the count before and after | `grep -c events_count ~/code/clients/cfta/data/.claude/worktrees/festive-heyrovsky-3bc06f/scripts/events/et_push.py` → was 0, expect at least 2 |
| 2026-09-25 | Nothing in Prism records that an event manager has reviewed a BEO, and nothing anywhere stops a rebuild from replacing a page the manager has finished with. On the Event Temple side the page now reads a completed task named "BEO review" on the booking (the same task mechanism carries any manual item, Natalie's ask), so Event Temple needs no new field. Still proposed: a `BEO status` picklist (Draft, Reviewed by event manager, Final) on the Prism event, and the rule that at Final the generator stops rewriting the Doc and drops a dated PDF into the BEO folder, which also needs a home on the Event Temple side (a task named "BEO final" would do). Creating a Prism field is a source-system change and waits for Brett's yes. | Brett (decision), then me (field and generator) | the Prism field exists and `beo.py` reads it, the freeze rule is coded, or Brett decides the review stays out of band | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from eventtemple.tasks where task_name ilike '%beo%' and is_completed\""` → 0 today; the Prism field is checked in the app |
| 2026-09-25 | The BEO Docs are rebuilt only when `beo.py` runs from a Mac, so the page's promise, that a fact recorded in Event Temple prints on the next rebuild, has no schedule behind it. The nightly rebuild needs the generator running on cfta-vm after the 06:30 spine build (the sync container has the Drive credentials; the data repo's scripts are not in the image yet). Until then Brett runs `beo.py --tree --upcoming 42 --format both` when a page should refresh. | me | a cron line on cfta-vm rebuilds the 42-day window nightly and logs to `/var/log/cfta-sync/beo.log` | `ssh cfta-vm "sudo grep -c beo /etc/cron.d/cfta-sync"` → 0 today |

## Closed

| Opened | Closed | Item | How |
|---|---|---|---|
| 2026-09-25 | 2026-09-25 | The BEO said the bar had not been counted since 21 July 2026 and asked for a fresh count. | Wrong premise, the agent's: the bar team counts monthly into the sheet FY26 - Bar Inventory (`1ZqZO37_xGKRi-jxP8fSZdh8YHVDNM3W3PXXLtoeGmbM`), with September the newest tab at 119 products; only the warehouse's copy had stopped at April. Fixed the same evening: cfta-sync module `bar_inventory` mirrors every month tab daily (on `land/beo-source-fields`, run once on cfta-vm, 1,009 rows), migration 096 adds `bar.v_inventory_latest`, and the BEO prints the newest month by family against what the event ordered. Verify: `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count_month, calendar_year from bar.v_inventory_latest limit 1\""` → September 2026. |
| 2026-09-25 | 2026-09-25 | The Google Slides API was not enabled in the service account's Cloud project (259219286444), so the floor plan preview could not start. | Brett enabled it the same evening. The probe then proved the design: shapes on a slide's layout render under the slide and cannot be selected or dragged from it, sizes read back to the EMU, and the one thing the API cannot do is set a page size, which is done by hand once per base file (File, Page setup, Custom). Two bases exist in `Events/Floor plan bases` on the sandbox (Steddy flat floor 17 by 16 in, Grace Atrium core 12 by 17 in) and the COSA reception draft is built from the Atrium base with every piece at its true size; `scripts/events/floor_plan.py` in cfta-data. |
| 2026-09-24 | 2026-09-24 | The email wordmark `assets/cfta-wordmark.png` and the Dockerfile's `COPY assets/ ./assets/` existed only on cfta-vm and as uncommitted work in the main sync checkout, so an image built from any commit sent the Monday brief and the readiness email with a broken logo. | Committed as 7e4f544 and fast-forwarded onto `master` on Gitea and GitHub at Brett's yes. The PNG matches cfta-vm's byte for byte (sha1 8ebf6772) and the Dockerfile matches the deployed one exactly. The check written for this could not have closed it: `git ls-files` in the main checkout reads the branch checked out there, not `master`. The right check reads `master`: `git -C ~/code/clients/cfta/sync ls-tree --name-only origin/master assets/ | wc -l` → 1. The main checkout still holds the same two changes uncommitted on `hvac-audit-and-bar-recon`; they are now redundant, and git will refuse to merge `master` into that branch until they are discarded. |
| 2026-09-24 | 2026-09-24 | The weekly event readiness email was built and dry-run but not installed. | Installed at Brett's yes: `event_readiness.py` in `/opt/cfta/cfta-sync` with the image rebuilt, and a Monday 06:45 line in `/etc/cron.d/cfta-sync` after the Monday brief (backup `/opt/cfta/backups/cfta-sync.cron.bak-20260924-readiness`). The first one went to Brett at 13:39 MT on 2026-09-24 through the same runner cron uses, and `workspace.sync_log` recorded it as a success. The code is on cfta-sync branch `land/beo-source-fields` (1ba203b, ebc7f3b), not `master`, so it belongs to the drift item above. |
| 2026-09-24 | 2026-09-24 | Planning Pod's Stripe history was not in the warehouse, and the account was about to be closed. | Archived in full before the close to `/Volumes/shares/cfta/audit/stripe-planning-pod-archive/` (JSONL, SHA256SUMS). Loaded 2026-09-24 through the deployed `sync_stripe` upserts in one transaction, tagged `account = 'planningpod'`: 75 payment intents, 49 charges ($100,357.37, 2021-05-17 to 2025-08-06), 94 balance transactions netting to $0.00, 11 refunds, 38 payouts, 46 customers. Every count matched the archive. The `stripe` schema was dumped first to `~/backups/stripe-schema-2026-09-24-before-planningpod.dump` on cfta-vm. The account was then closed. |
| 2026-09-24 | 2026-09-24 | Inbox triage ran on weekdays at 6:45 AM and 1:45 PM: it sorted Brett's unread mail with the local model, wrote draft replies, and emailed the "Inbox Triage" digest. | Stopped at Brett's request. Both lines in `/etc/cron.d/cfta-sync` are commented out (backup `/opt/cfta/backups/cfta-sync.cron.bak-20260924-triage`), and its entry in the Morning Briefing's freshness list is commented out too, so the briefing does not report it as stale (backup `morning_briefing.py.bak-20260924-triage`, image rebuilt). Drafts it already wrote stay in Gmail. |
| 2026-09-24 | 2026-09-24 | The CFTA Sync Errors email went to Brett whenever a sync run failed, one to three a week lately and 54 in a single day during the 2026-08-11 outage. | Stopped at Brett's request by removing `email=True` from the two failure alerts in the deployed `orchestrator.py` (backup at `/opt/cfta/backups/orchestrator.py.bak-20260924-syncerr`) and rebuilding the image. Failures still push to ntfy `cfta-alerts` and still show in the 07:00 Morning Briefing. The Data Volume Alert email was left on. The change is live only on the VM; see the drift item above. |
| 2026-09-24 | 2026-09-24 | The info@ inbox digest emailed twice a day at 6 AM and 2 PM. | Stopped at Brett's request. Its line in root's crontab is commented out with a dated note; backup at `/root/crontab.root.20260924-065253.bak`. Nothing else schedules or calls it. |
| 2026-09-22 | 2026-09-24 | The Wine and Food festival jobs kept running after the festival: the reply digest email twice a day, the reply scan every half hour, `wff-gt-watch` every ten minutes, and `wff-chat-bridge` every two. | Parked at Brett's request. All four lines in root's crontab are commented out with a dated note, so next year they come back by removing the `#`. Backups at `/root/crontab.root.20260924-064859.bak` and `-065118.bak`. The two daily sheet uploads in `/etc/cron.d/cfta-sync` were already off through `WFF_DAILY_ENABLED` and were left alone. |
| 2026-09-23 | 2026-09-23 | The `db-write-guard.sh` false-positive fix was only on a side branch, and the hook runs from the main checkout. | Fast-forwarded main and `report/fincmte-fy26-board-revisions`, the branch the main checkout is on. The live hook passes all 18 cases. The first check written for this asked only whether main had it, which would have closed the item while the old guard was still live. The right check reads the file that runs: `grep -q 'WHAT IT SKIPS' ~/code/claude-skills/hooks/db-write-guard.sh`. |
| 2026-09-23 | 2026-09-23 | "No Humanitix ticket revenue line carries an event customer." | Wrong, and dropped. All 45 lines carry one. The claim came from querying `qbo.general_ledger.name`, which is the payee on bills and expenses and is empty on journal entries by design; the customer is in `cust_name`. The tktgross contra lines, which had been built without customers on that belief, were corrected to carry them. |
| 2026-09-22 | 2026-09-22 | The Bloomerang cleanup toolchain lived only on a worktree branch, 13 commits, never on main. | Merged (23d6654). Migration 048 had been applied by hand and never recorded; it went through the runner and migrations-check shows no drift. The runner itself turned out never to work from a clean checkout (tracked 100644, hidden by core.fileMode=false), fixed in ed03c11. The 22 gitignored working files, 23 MB, were copied into the main checkout byte-identical, so the worktree can go. |
| 2026-09-22 | 2026-09-22 | The Event Temple Stripe account had never been in the warehouse, while 26,562.61 moved through it in September alone. | The `stripe` module syncs a list of accounts now and tags every row with `account`. `acct_1T1x2mA5D7KNjxjD` is connected on a restricted read key. 392 records backfilled to 2026-02-27, and all five September payouts tie to the bank to the penny. The first key made was an organization-level secret key that could not reach any account; it was replaced with a restricted one. |
| 2026-09-22 | 2026-09-22 | Every job in `/etc/cron.d/cfta-sync` ran six hours early, because the file declared `CRON_TZ=America/Denver` and this cron ignores that variable. | Host timezone set to America/Denver, which is the only lever this cron has. No schedule line moved; the two digest lines in root's crontab, which were written in UTC on purpose, became 6 and 14 local. Postgres stays on Etc/UTC by explicit setting. `cron-tz-assert.sh` now checks the host clock daily, because the comment that was supposed to guarantee this is what failed. |
| 2026-09-21 | 2026-09-22 | The board companion disagreed with the working workbook on eleven figures. | Rebuilt; the consistency check prints clean across every surface. |
| 2026-09-05 | 2026-09-22 | The wine pipeline had been failing since on or before 2026-09-05 on a stale password. | Ported to the `wine` module in cfta-sync, scheduled daily at noon, monitor healthy. |
| 2026-09-22 | 2026-09-22 | 186 seat reservations held by orders that were never sales, inflating 17 sessions. | Migration 086 removed them, with a copy kept; the sync and the monitor now exclude those order statuses. |
| 2026-09-22 | 2026-09-22 | The n8n workflow `sync-wine-woocommerce` was still active after its work moved to the `wine` module. | Deactivated during the first run of this review, which exceeded its brief by acting rather than reporting; the task prompt now forbids that. Verified independently: `active` reads `f`, the n8n log records "Deregistered all crons for workflow", and it has not fired since. One foreign key error on `workflow_history` appeared in the same window and has not recurred; n8n ran 101 other executions in the following half hour. |
| 2026-09-22 | 2026-09-22 | Eleven pipelines read stale in ops.v_sync_health and nobody was acting on the list. | Triaged: they were not eleven problems. Six exempt (retired, superseded, or one-off), four given real cadences, and two that had working modules nobody ever scheduled are now in cron and verified by running them. Migrations 088 and 089. Stale went from eleven to zero. |
| 2026-09-22 | 2026-09-22 | The nightly dbt run had failed every night on a repository path that moved in August, leaving the marts stale. | Found during the triage. Path corrected on the deployed copy with a backup; verified it resolves as the user cron runs as. The marts refresh on tonight's run. |
