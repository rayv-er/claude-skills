# Open items

Things handed back and not yet closed. One line each, with an owner, the date it opened, what closing it
looks like, and where possible a command that answers "is this still open" without asking anyone.

The rule that makes this worth keeping: **an item leaves this list by being done or by being decided
against, and both are closures.** An item nobody will ever do, left open, is what turns a list like this
into noise. Move it to Closed with `dropped` and the reason.

Reviewed every Monday morning by the `coach-open-items` scheduled task, which runs the verify commands,
closes what it can prove is done, and reports only what is genuinely still open.

## Open

| Opened | Item | Owner | Closed when | Verify |
|---|---|---|---|---|
| 2026-09-22 | Credential rotation has not started. Order is in `docs/runbooks/credential-rotation.md`, with the Google service-account key first because it reads every mailbox, then the vendor keys, then the database roles. The `dbt_runner` password is also a literal in the sync repository's `deploy/dbt-run.sh`. | Brett and admin | every credential in the runbook rotated and the store re-encrypted | manual: ask, and check `git log -1 --format=%as -- secrets/` in cfta-data |
| 2026-09-22 | Seventeen `wine.ticket_sales` rows belong to orders that were cancelled, abandoned, or failed, four of them marked paid. They hold no seats any more. Removing them is a decision nobody has made. | Brett | rows removed, or decided to keep them and this line dropped | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from wine.ticket_sales ts join wordpress.passes p on p.pass_key=ts.pass_key join wordpress.orders o on o.id=p.order_id where o.status in ('checkout-draft','failed','cancelled')\""` → was 17 |
| 2026-09-22 | The daily 08:45 Hetzner push has not completed since before 20 August. The exec bit was lost and git could not see it (`core.fileMode=false`), which killed 33 consecutive runs; the cron now invokes it through `/bin/bash`. The run before that already failed to push all 25 schemas, and that cause is still unknown, so dash.cbarts.center is serving stale data. | me to diagnose, Brett to confirm the dashboard matters | a run completes with no schemas listed as not pushed | `grep 'schema(s) did not push' /tmp/push-to-hetzner.log | tail -1` → expect no output; last run listed 25 |

| 2026-09-22 | Nobody has put a utility bill in the Drive folder since July. The sync job is healthy and ingested 9,114 rows on 2026-09-22; it pulled zero files. Deliberately not exempted so it keeps saying so. | Brett | bills resume, or a decision that they will not and the schema is exempted | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select round(age_hours/24) from ops.v_sync_health where schema_name='utility'\""` → was 54 days |
| 2026-09-22 | Two stale database passwords in n8n, not one. Three live workflows fail authenticating as `cfta_sync`: `health-check`, `summarize-donor-interactions`, `prospect-state-analysis`. One fails as `postgres`: `Idea Deep Research`, 307 errors. The working `cfta_sync` value is in `/opt/cfta/cfta-sync.env` on cfta-vm; the live `postgres` value is not in the sops store and belongs to the rotation. health-check being dead is why none of this reports itself. | Brett | those four workflows record a success | `ssh cfta-vm "sudo runuser -u postgres -- psql -d n8n -At -c \"select count(*) from execution_entity e join workflow_entity w on w.id=e.\\\"workflowId\\\" where w.active and e.status='error' and e.\\\"startedAt\\\" > now() - interval '1 day' and w.name in ('health-check','summarize-donor-interactions','prospect-state-analysis','Idea Deep Research')\""` → was 319 |
| 2026-09-22 | The `dbt-run.sh` path fix exists only as uncommitted work in the sync checkout, and that file also carries the `dbt_runner` password as a literal. The deployed copy was corrected on cfta-vm. | whoever owns that branch | the fix is committed and the literal is gone | `cd ~/code/clients/cfta/sync && git status --porcelain deploy/dbt-run.sh` → expect empty |
| 2026-09-22 | The Wine and Food reply digest still runs twice a day and its scan every half hour, with 0 upcoming dinners and 0 recipients since the festival ended. It writes an unchanged sheet each time and would mail an empty digest. Parking it until next year's festival is a decision nobody has made. | Brett | the jobs are parked, or a decision to leave them running and this line dropped | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm cfta-sync python wff_reply_digest.py --dry-run 2>&1 | grep Found"` → was 0 dinners, 0 recipients |
| 2026-09-22 | Every secret in the cfta-sync service environment was printed into a session transcript on this Mac, by running `docker compose config` without filtering the output. The values are the same ones already in `/opt/cfta/cfta-sync.env` and the sops store, so this adds one more copy at `~/.claude/projects/`, on disk, unencrypted. It is a reason to move the rotation up rather than a breach on its own. | Brett and admin | the rotation above is done, or the transcripts holding it are removed | `grep -rl CLOVER_TOKEN ~/.claude/projects/ 2>/dev/null | wc -l` → expect 0 |
| 2026-09-22 | Root's crontab on cfta-vm holds 33 scheduled jobs, including the nightly backup, the spiders, and the two inbox digests, and it is not in version control anywhere. The 2026-07-31 pass moved some of its jobs into `/etc/cron.d/cfta-sync` and stopped partway. A rebuilt VM loses all 33 silently. | Brett to say whether they move into the cron file or get their own tracked file | every line is in a file under git | `ssh cfta-vm "sudo crontab -l -u root | grep -c '^[0-9*]'"` → was 33, expect 0 once they have moved |
| 2026-09-22 | The canonical event dimension is built and verified (1,054 events FY22-FY26, migration 090) but only 77 of the 761 without a QuickBooks customer have any money at stake, and 25 of those carry 87% of it. The review sheet is sorted by money so the read is 25 rows, not 761. The review of proposed renames, creates, and the ten dated-vs-undated splits is the next decision, and nothing may be written to QuickBooks until it happens. | Brett | a reviewed sheet exists and the rename/create list is accepted or rejected | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from events.dim_event where qbo_customer_id is null\""` → was 761, of which 77 carry money |
| 2026-09-22 | `scripts/budget/attribution_scan.py` should target `events.dim_event` instead of raw customer names. It has the tuned matcher, thirteen grades, and stored confirmations; the dimension has the canonical targets. Until they are joined, line-level attribution proposals still come from name matching against 381 customers rather than 1,454 events. | me | attribution_scan proposes an event_key alongside its customer | `grep -c dim_event ~/code/clients/cfta/data/scripts/budget/attribution_scan.py` → expect > 0, was 0 |
| 2026-09-22 | `sync_qbo.py` queries Customer with no WHERE clause, so `qbo.customers` can never hold an inactive record. The close standard's rule to search "inactive included" before creating a customer cannot be satisfied from SQL, which matters the moment the create list above is acted on. | me | the mirror holds inactive customers | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from qbo.customers where not active\""` → was 0 |
| 2026-09-22 | Julia created 2,528 constituents in Bloomerang on 2026-09-15, the day the cleanup sheet was built, under her own name rather than the API key. It tripled the duplicate list, from 343 pairs to 967, and "same name and street" alone went from 2 pairs to 306. Whether that load was meant to land before the deduplication, and whether any of it should be rolled back, is Brett's call. | Brett | a decision that the import stays, or a list of what comes back out | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from bloomerang.constituents where created_date::date = '2026-09-15' and created_by = 'Julia Brazell'\""` → 2,528 |
| 2026-09-22 | A third party writes to Bloomerang that nothing in our documentation mentions: TrueGivers [API] modified 3,222 records through 2026-09-20 and addresses flagged bad went from 464 to 650. Address verification is partly a vendor's job, which changes what the address step of the cleanup program should do. | Brett to confirm the arrangement | the vendor and its scope are written down, and the address step scoped around it | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select max(last_modified_date)::date from bloomerang.constituents where last_modified_by like 'TrueGivers%'\""` |
| 2026-09-22 | Stripe fees have been derived rather than actual on every reconciliation this year, and the actuals are now in the warehouse. The rate used in JE 34438 was 3.441%, taken from the payout rail. The actual rate on Event Temple charges runs 2.86% to 3.06% by month and sat at 2.857% in August. That is a different population from the JE's cutoff window, so it is a true-up to compute rather than a number to assume, but the data exists for the first time. | Brett to decide whether to restate, me to compute it | the derived-fee note comes off the Stripe recon | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select round(100.0*sum(fee)/sum(amount),3) from stripe.balance_transactions where account='main' and type='charge' and created_at >= '2026-08-01' and created_at < '2026-09-01'\""` -> 2.857 |
| 2026-09-22 | FY26 corporate sponsorship revenue is 7,000.00 on two lines, both W+FF, both with no customer on them. At least ten signed 2026 business sponsorship agreements exist in Drive, including SHM Architects at 15,000 and The Beckwith, and none is visible in the ledger by name. There is also no sponsorship or underwriting account in the chart at all; the two lines sit in `4040.12 Corporate`. The agreements run January to December and the fiscal year ends in October, so some of this is timing, but not all of it. | Brett and the CBO | every signed agreement is either booked or explained | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from qbo.general_ledger where txn_date >= '2025-11-01' and account_name='4040.12 Corporate' and coalesce(name,'')=''\""` -> was 2 unnamed lines |
| 2026-09-22 | `workspace.drive_files.text_path` points at extracted text that no longer exists on disk. Nothing is at `/data/drive_text` in the container or on the host, so a column that says a document is searchable is lying, and any search built on it silently returns nothing. Found while reading the sponsorship agreements, which had to be pulled through the Drive API instead. | me | the column is either repopulated or dropped | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm cfta-sync sh -c 'ls /data/drive_text 2>&1'"` -> no such directory |
| 2026-09-23 | `deploy-overwrite-guard.py` blocks installing a file that does not exist on the server yet, where there is nothing to overwrite and nothing to lose. It fired twice today on a new script. The fix is to let a create through, or to ask for a backup only when the destination exists. A guard that fires on safe work trains people to reach for GUARD_OK. | me | a new-file install passes without an override | `grep -n 'writes_into_deployment' ~/code/claude-skills/hooks/deploy-overwrite-guard.py` |
| 2026-09-23 | 22,627.80 of FY26 giving is still on 4030.13 Temporarily Restricted. Everything else was released or reclassified on 2026-09-23 by Bloomerang designation (releases 34777 to 34780 and 34783, the gallery on its donors; reclassifications 34781, 34782). **Held by decision:** the Walter Family Foundation's 20,000 (Brett: leave restricted). **Waiting on a fact:** two Bloomerang payouts with no donor, 48.10 on 15 July and 2,419.40 on 27 July, which Bloomerang's payout report would split; a 110.30 tip and a 50.00 Humanitix payout for Apres Jazz that are not gifts. | Brett: the payout report, where the two non-gifts go | each line released, reclassified, or held with a named reason | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select sum(amount) from qbo.general_ledger where txn_date >= '2025-11-01' and account_name ~ '^4030'\""` -> 22,627.80 once the warehouse syncs |
| 2026-09-23 | Two FY26 bar sales-tax filings do not fit the bar sales they cover. May remitted 94.08 against 6,070.50 of bar sales, where 9.4/109.4 would be 521.60. July remitted 6,139.69 more than bar tax alone; June's entry netted out 826.70 of Alpenglow food-vendor tax and July may not have. Found while spreading the carve-out; the differences now sit visibly on Programming - Overhead, one line a month. | Brett or the CBO, against the filed returns | each difference is explained or corrected | the residual lines on `YYYY-MM-bartax` for 2026-05 and 2026-07, description beginning "bar sales tax - filed" |
| 2026-09-24 | BEOs from Prism: `scripts/events/beo.py` in cfta-data builds a Google Doc per show in Brett's BEOs folder, and the Floodgate Operators pilot is built. Two Prism admin steps remain: add a "BEO" custom field of the URL type, like the existing "Facebook URL", so each event can link its Doc; and create a Prism API key with Read & Write (write-events) so the generator can attach the BEO to the event. The key goes into the sops store as PRISM_RW_API_KEY, never into chat. | Brett or a Prism admin | the field exists and the key is in the sops store | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from prism.event_financials f, jsonb_array_elements(f.raw::jsonb -> 'customFields') c where c ->> 'label' = 'BEO'\""` → was 0; `grep -c PRISM_RW_API_KEY ~/.config/cfta/db.env` → was 0 |
| 2026-09-24 | After rentals and co-pros got their revenue on 2026-09-24 (39,771.25 onto 11 events), six dated programming events still carry costs and no revenue: the CBCS Band Concert (3,031), Graduation (2,053), and Jazz (792), which may have been free community use; SOD Nutcracker Rehearsals (1,443), which belong with the Nutcracker; the 2025 Arts Ball's FY26 costs (6,549); and Event Staff Training (949), which is not an event and can stay. Smaller: 6,480 of December revenue on Rentals - Overhead from FY25 deposits, composition partly documented; 614 of March hosted bar unattributed (Winter Tech Forum?); Eleven Experience's 946.72 hosted bar at Alpenphunk on the renter; and whether Blackline's 16,200 deposit, now spread over the nine Alpenglow shows, was netted against its 4,200 per-show bills. | Brett | each placed on its event, confirmed free or co-produced, or left with a reason | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"with r as (select regexp_replace(g.cust_name, '^((Programming:Rentals|Programming):[^:]+).*$', '\\1') ev, a.classification c, g.amount from qbo.general_ledger g join qbo.accounts a on a.account_id = g.account_id where g.txn_date >= '2025-11-01' and g.cust_name ~ '^Programming:') select count(*) from (select ev from r where split_part(ev, ':', -1) ~ '^20[0-9]{2}[.]' group by ev having sum(amount) filter (where c = 'Expense') > 500 and coalesce(sum(amount) filter (where c = 'Revenue'), 0) = 0) x\""` → was 6 |
| 2026-09-23 | The monthly restricted fund check (`restricted_fund_check.py` in cfta-sync, cron the 2nd at 07:20, mail to Brett only) found on its first run where QuickBooks and Bloomerang still disagree, beyond the 22,627.80 above. **A grant:** Colorado Creative Industries 7,500 on 30 June, booked to 4020.11 restricted, Unrestricted in Bloomerang; the award letter decides which is right. **Designated in Bloomerang, booked unrestricted:** six lines, 11,891.71, of which 10,331.18 is one donor's 5,165.59 Alpenglow gift entered on 2 and 18 December, likely once too many. **Restricted grants on 4020.11 not yet released:** Mountain Words 2,740 and Dance 3,460. **In Bloomerang to a restricted fund, not found in QuickBooks:** seven gifts, the largest Inn Old Town 5,500 to Mountain Words. | Brett | the check reads 0 to look at, or every remaining line is held with a named reason | `ssh cfta-vm "cd /opt/cfta && sudo docker compose run --rm cfta-sync python restricted_fund_check.py 2>/dev/null | head -1"` -> was "10 to look at" |
| 2026-09-24 | Floodgate Operators' Prism contact for Rebecca Vehik gives her email as `rebecca@crestedbutte.org`; her Event Temple login is `rebecca@crestedbuttearts.org`. BEOs now take staff emails from Event Temple, so the BEO is right, but Prism's own PDFs and emails still carry the typo. It is the only CFTA staff email in Prism that disagrees. | Brett, or whoever keeps Prism contacts | the Prism contact is corrected and the nightly sync has run | `ssh cfta-vm "sudo runuser -u postgres -- psql -d cfta_data -At -c \"select count(*) from prism.event_contacts ec join eventtemple.users u on lower(u.name) = lower(ec.name) where ec.company ~* '^(center for|cfta)' and lower(ec.email) <> lower(u.email)\""` → was 1, expect 0 |

## Closed

| Opened | Closed | Item | How |
|---|---|---|---|
| 2026-09-23 | 2026-09-23 | The `db-write-guard.sh` false-positive fix was only on a side branch, and the hook runs from the main checkout. | Fast-forwarded main and `report/fincmte-fy26-board-revisions`, the branch the main checkout is on. The live hook passes all 18 cases. The first check written for this asked only whether main had it, which would have closed the item while the old guard was still live. The right check reads the file that runs: `grep -q 'WHAT IT SKIPS' ~/code/claude-skills/hooks/db-write-guard.sh`. |
| 2026-09-23 | 2026-09-23 | "No Humanitix ticket revenue line carries an event customer." | Wrong, and dropped. All 45 lines carry one. The claim came from querying `qbo.general_ledger.name`, which is the payee on bills and expenses and is empty on journal entries by design; the customer is in `cust_name`. The tktgross contra lines, which had been built without customers on that belief, were corrected to carry them. |
| 2026-09-22 | 2026-09-22 | The Bloomerang cleanup toolchain lived only on a worktree branch, 13 commits, never on main. | Merged (23d6654). Migration 048 had been applied by hand and never recorded; it went through the runner and migrations-check shows no drift. The runner itself turned out never to work from a clean checkout (tracked 100644, hidden by core.fileMode=false), fixed in ed03c11. The 22 gitignored working files, 23 MB, were copied into the main checkout byte-identical, so the worktree can go. |
| 2026-09-22 | 2026-09-22 | The Event Temple Stripe account had never been in the warehouse, while 26,562.61 moved through it in September alone. | The `stripe` module syncs a list of accounts now and tags every row with `account`. `acct_1T1x2mA5D7KNjxjD` is connected on a restricted read key. 392 records backfilled to 2026-02-27, and all five September payouts tie to the bank to the penny. The first key made was an organization-level secret key that could not reach any account; it was replaced with a restricted one. |
| 2026-09-22 | 2026-09-22 | Every job in `/etc/cron.d/cfta-sync` ran six hours early, because the file declared `CRON_TZ=America/Denver` and this cron ignores that variable. | Host timezone set to America/Denver, which is the only lever this cron has. No schedule line moved; the two digest lines in root's crontab, which were written in UTC on purpose, became 6 and 14 local. Postgres stays on Etc/UTC by explicit setting. `cron-tz-assert.sh` now checks the host clock daily, because the comment that was supposed to guarantee this is what failed. |
| 2026-09-21 | 2026-09-22 | The board companion disagreed with the working workbook on eleven figures. | Rebuilt; the consistency check prints clean across every surface. |
| 2026-09-05 | 2026-09-22 | The wine pipeline had been failing since on or before 2026-09-05 on a stale password. | Ported to the `wine` module in cfta-sync, scheduled daily at noon, monitor healthy. |
| 2026-09-22 | 2026-09-22 | 186 seat reservations held by orders that were never sales, inflating 17 sessions. | Migration 086 removed them, with a copy kept; the sync and the monitor now exclude those order statuses. |
| 2026-09-22 | 2026-09-22 | The n8n workflow `sync-wine-woocommerce` was still active after its work moved to the `wine` module. | Deactivated during the first run of this review, which exceeded its brief by acting rather than reporting; the task prompt now forbids that. Verified independently: `active` reads `f`, the n8n log records "Deregistered all crons for workflow", and it has not fired since. One foreign key error on `workflow_history` appeared in the same window and has not recurred; n8n ran 101 other executions in the following half hour. |
| 2026-09-22 | 2026-09-22 | Eleven pipelines read stale in ops.v_sync_health and nobody was acting on the list. | Triaged: they were not eleven problems. Six exempt (retired, superseded, or one-off), four given real cadences, and two that had working modules nobody ever scheduled are now in cron and verified by running them. Migrations 088 and 089. Stale went from eleven to zero. |
| 2026-09-22 | 2026-09-22 | The nightly dbt run had failed every night on a repository path that moved in August, leaving the marts stale. | Found during the triage. Path corrected on the deployed copy with a backup; verified it resolves as the user cron runs as. The marts refresh on tonight's run. |
