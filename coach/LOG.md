# Coaching log

One line per suggestion, so coaching compounds instead of repeating itself. A suggestion that was
declined is not raised again; a suggestion that was adopted becomes the assumed standard and stops being
mentioned. Status is one of: introduced, practising, adopted, declined.

Read this before offering anything. Add to it when something is offered, and update the status when it
changes.

| Date | Curriculum item | Suggestion | Status |
|---|---|---|---|
| 2026-09-22 | 1. State before acting | Before a DELETE or UPDATE, check the table has a key, run it as a SELECT and read the count, keep a copy in the same transaction. Now enforced by the database guard. | introduced |
| 2026-09-22 | 1. State before acting | Diff a deployed file against the local one before overwriting it; the server's copy is sometimes ahead. Now enforced by the deploy guard. | introduced |
| 2026-09-22 | 8. The read-only path | Ask question-shaped things in chat with the tool server rather than opening a session. Six finance tools added so it reaches the finance work. | introduced |
| 2026-09-22 | 2. Branch discipline | The sync repository is on a feature branch, one ahead of the default and eight behind, with 25 uncommitted files. Worth an hour to land or drop. | introduced |
| 2026-09-22 | (agent defect) | Email bodies were hard-wrapped, twice. Fixed in tooling rather than by instruction: email-wrap-guard.py blocks a wrapped body on the mail tools, and brett-prose carries the rule. | adopted |
| 2026-09-22 | (agent defect) | Told Brett the 13:45 inbox triage would reach him, from the file being fixed rather than from the scheduler. It had already run at 07:45, because /etc/cron.d/cfta-sync declares a timezone Ubuntu's cron ignores. Claiming a delivery time needs the job's own last log line, not the crontab's comment. | introduced |
| 2026-09-23 | 2. Branch discipline | Keep `~/code/claude-skills` on main. Hooks and skill symlinks load from that checkout, so a fix merged to main did nothing while it sat on a report branch; the db guard fix needed two fast-forwards to go live. Switched the same day. | adopted |
| 2026-09-23 | (agent defect) | Told Brett no Humanitix revenue line carried an event customer, then that the warehouse dropped customers on journal entries. Both wrong: the query read `qbo.general_ledger.name`, the payee, instead of `cust_name`. Built ten journals without customers on that belief, then amended them. Same session, bucketed Humanitix events by UTC month and put Jerry Garcia's 31 January night in February. Before concluding a field is empty, read the source object once; before grouping by month, convert to America/Denver. | introduced |
| 2026-09-24 | (agent defect) | Moved two rentals-journal lines to the wrong side. A rentals entry pairs its revenue credit with a deferred-revenue debit carrying the same description and amount, and the matcher took whichever came first, so CB Avalanche Center's and Move The Butte's debits moved and their revenue did not. The second run called both "already on the event" because it checked the same wrong line, and I told Brett every event's revenue added up without checking Move The Butte's. Caught reading the rebuilt sheet, repaired the same day. Fixed in tooling: qbo_event_revenue.py now matches revenue credits only and restored the two debits. A check that a move landed has to read the account that moved, not the line the script touched. | adopted |
| 2026-09-24 | (agent defect) | Rebuilding the Floodgate BEO Doc overwrote the Floodgate BEO sheet: the Doc's lookup matched on the Prism event id alone, and the sheet built an hour earlier carries the same id. Caught from the run's own output ("rewritten" pointing at a spreadsheet URL), restored the same day by copying the template's tabs back and refilling; revision history showed no one had edited it. Fixed in tooling: the Doc lookup requires kind beo and the Doc type. Two artifacts sharing a key need the lookup to say which one it wants. | adopted |
| 2026-09-24 | (agent defect) | Labelled each rental BEO's Event Temple booking owner as its event manager, from the field's name rather than its use. The owner is whoever sold the booking, so the Gibson BEO named Dana Cowley where the booking notes say Amanda Bade. Caught while wiring in the booking notes. Fixed in tooling: beo.py takes the event manager from the Event Coordinator field, the operations user, a note that names one, or an owner Event Temple titles Event Manager, and prints the owner as sales. A field's name is a guess about what people put in it; read a few records against a second source before labelling with it. | adopted |
| 2026-09-24 | (agent defect) | Stamped each BEO Doc's event type under the key `kind`, which is the key that marks a file as a BEO Doc, and overwrote it on all twelve. The readiness email's dry run then found no BEOs, and beo.py's next run would have created twelve duplicates. Repaired within minutes by finding each Doc by its new value and restoring `kind: beo`; the next run rewrote all twelve in place. Fixed in tooling: the stamp is `event_type`, and put_doc writes the id and kind last so no stamp can replace them. A key that identifies a record is not free to reuse for data. | adopted |
| 2026-09-24 | (agent defect) | Emptied `coach/OPEN.md` in the working tree while adding a row, with `open(p, "w").write(patch(open(p).read()))`: Python opens the file for writing, which truncates it, before it evaluates the argument that reads it. The file held another session's 11 uncommitted lines. No snapshot or git blob held them; rebuilt from the diff printed earlier in the session and confirmed at the same 11 lines, and both sessions told. Both sessions confirmed every row intact; their last edits were at 07:05 and 08:15 MT, before the diff, so nothing was lost. The same session's commit d6bbe30 claimed this log entry and did not contain it, because the script died before reaching it and the next command ran anyway. Read a file into a variable before any open for writing, and chain a script and the commit that depends on it with `&&`. | introduced |
| 2026-09-25 | (agent defect) | The lodging sync, written 2026-07-31, read brett@ alone because one measurement found brett@ holding "every confirmation", and its docstring said so. Eight mailboxes held them; for two months the table was missing 27 reservations, seven of them 2026 stays including the Wine + Food coordinator's week, and held six June rooms at two nights after the hotel cut them to one. Found only because the Monday brief showed no lodging and Brett asked where it came from. Fixed in tooling: the sync reads every staff mailbox and the Gmail archive and logs how many sources it searched, and the read-only check before the deploy compared every existing row. A measurement of one source says what that source holds, not that nothing is elsewhere; count the other places before writing "every". | adopted |
